.git
.github
.next
node_modules
npm-debug.log*
.env*.local
.env
*.md
.dockerignore
Dockerfile
docker-compose.yml
# Prometheus/Grafana config is mounted from the host, never baked into the image.
monitoring
# Report PDFs; large and irrelevant to the build.
reference
